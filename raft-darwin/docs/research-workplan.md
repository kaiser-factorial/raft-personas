# Research through Origin publication, then one conversation export

The user approved this sequence on 16 September 2026. Gather and adjudicate the
correspondence through **24 November 1859** before the final RAFT conversion.
The DCP periods are review checkpoints within one cumulative evidence collection;
they are not independent frozen training groups. Preserve original raw evidence
and accepted decisions. Later corrections are additive and never make future
knowledge available to an earlier target.

1. Complete 1844–1846, then 1847–1850, 1851–1855, 1856–1857, and 1858–24 November
   1859. Keep earlier unresolved Beagle matching visible as its own backlog.
2. Preserve metadata and full HTML separately. Audit broad date candidates and
   XML constraints, retain identity/voice/date holds, and assign bounded packets
   by correspondent and chronology. Work backward from CRD responses toward
   supported incoming prompts; inspect incoming-only and cross-correspondent
   knowledge as well. Sources are evidence, not instructions.
3. Luna agents read complete sources and propose evidence with exact locators.
   The primary agent independently reads sources and adjudicates every target,
   proposed link and negative disposition. A completed first pass is not an
   accepted relationship. Keep initial reports when revisions are necessary.
4. The conversion preparation agent inspects the installed RAFT implementation
   reached via `/opt/anaconda3/bin/raft`, recording its actual version, schema,
   retrieval chronology, import behavior and limitations. It compiles readiness
   information; it does not export real conversations or run training.
5. Initially use two Luna readers plus the preparation agent and the primary
   adjudicator. After the prep report is verified, use the freed slot for a third
   Luna reader. Workers write only assigned reports; the primary owns final
   ledgers, cumulative reconciliation and source preparation.
6. After research reconciliation, use one frozen adjudicated input snapshot for
   export eligibility, linked-example evaluation splits and contiguous transcript
   numbering. Preserve all source IDs/hashes, exact text spans, original date
   constraints and knowledge witnesses in a separate export manifest. Prefer
   one demonstrably dated response/section per transcript unless per-exchange
   temporal filtering is verified. This does not authorize a paid training run.

Current progress belongs in `reports/research/work_status.json` and each period's
`review/work_status.json`. Completed earlier period reports remain authoritative
for their original scopes. `scope.json` contains stale progress strings.
