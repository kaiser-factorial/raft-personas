# 1858 to 24 November 1859 source staging

773 metadata candidates were inspected; 707 selected source records have preserved
HTML bodies, with zero fetch errors. Their raw classifications are 367 within the
period, 275 ambiguous and 65 later than the endpoint. The later records remain
boundary evidence and are not eligible as prepublication training responses.

There are 489 Darwin-origin records and 218 incoming records; 299 require exact
date review. These are source-inventory counts, not accepted correspondence pairs.
The selection scans the full catalogue for candidate dates and reuses preserved
XML bounds; it is not a fresh XML audit of every catalogue record.

The staging audit checked all 773 metadata files, all 707 bodies and selected XML
provenance. The primary independently verified all 707 selected HTML/XML hash
pairs and the 65 later lower bounds. See `source-staging-audit.json`, `summary.json`
and the raw manifests for details.

No reading packets, adjudication or conversation export have been performed for
this period. Refresh the context projection after prior-period reconciliation
before constructing correspondent reading packets. Final conversion will use one
cumulative adjudicated snapshot after research through the endpoint.
