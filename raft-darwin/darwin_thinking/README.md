# darwin_0 prepared data

One validated cumulative export through **24 November 1859**:

- **162** single-exchange transcripts in `conversations/transcript-0001.json` through `transcript-0162.json`.
- **1,405** dated Darwin grounding documents in `corpus/documents.jsonl`.

Read the [export report](../reports/raft-export/README.md) for provenance, content/date policy, validation and held material. The [manifest](../reports/raft-export/manifest.json) pins the files. No embeddings or training have been run; no benchmark has been selected.
